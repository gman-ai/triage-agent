# triage-agent

Per-tenant SecOps alert triage with grounded reasoning, tiered cost-controlled routing, and structured analyst-grade output.

## Status

Work in progress. Build scaffolding only.

## Reproduction

```bash
uv sync
uv run pytest
uv run eval
```

The full design and rationale will be documented in `DESIGN.md` once the build lands.
